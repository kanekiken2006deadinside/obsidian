
> «TCP/IP — это набор правил, по которым компьютеры общаются в сети. Он делает две вещи: даёт каждому машине адрес и доводит данные до нужного адреса через промежуточные узлы.»

Аналогия, если попросит проще: **это почта.** IP-адрес — адрес на конверте, маршрутизация — логистика, которая решает, через какие сортировочные центры везти письмо.

Ещё две вещи на всякий случай:

- **IP отвечает за адрес и путь, TCP — за надёжность**: разбивает данные на части, следит, чтобы всё дошло и собралось в правильном порядке.
- **В Linux ставить его не надо**, он встроен в ядро. Поэтому я просто показываю `ip a`: есть строка `inet` с адресом — значит стек работает.



IP a 
**`ip a` — шпора**

**`lo`** — localhost
- IPv4: `127.0.0.1/8` · IPv6: `::1/128`
- MAC: все нули · forever
**`wlo1`** — Wi-Fi
- `UP`+`LOWER_UP` = работает
- MAC: `04:e8:b9:35:07:5f` · alt: `wlp0s20f3`
- IPv4: `192.168.1.152/24` (DHCP)
- IPv6: `fe80::.../64` (link-local)
**Флаги:** `UP` вкл · `LOWER_UP` есть связь · `LOOPBACK` петля · `BROADCAST`/`MULTICAST` рассылки
**Адреса:** `inet`=IPv4 · `inet6`=IPv6 · `link/ether`=MAC
**Scope:** `host` внутри ПК · `link` в сегменте · `global` везде
**Прочее:** `mtu` размер пакета · `dynamic`=DHCP · `valid_lft` срок · `brd` broadcast
**Команды:** `ip -4 a` IPv4 · `ip -6 a` IPv6 · `ip a show wlo1` один интерфейс



**`ip a` (`ip address`) — команда Linux для просмотра сетевых интерфейсов и их IP-адресов.**

**Зачем нужна:**
- Узнать свой **IP-адрес** (локальный/внешний)
- Проверить, **включён ли интерфейс** (`UP`/`DOWN`)
- Найти **MAC-адрес** сетевой карты
- Проверить, **получен ли адрес по DHCP** или задан вручную
- Диагностика проблем с сетью (нет адреса = нет интернета)

**Где пригодится:**
- Подключение к роутеру / настройка сети
- Проброс портов, SSH, локальные серверы
- Проверка «почему не работает Wi-Fi»

**Аналоги:** `ifconfig` (устаревшая), `ipconfig` (Windows).

Коротко: **показывает, как компьютер подключён к сети.**